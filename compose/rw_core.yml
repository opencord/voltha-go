version: '2'
services:
  rw_core:
    image: voltha-rw-core
    entrypoint:
        - /app/rw_core
        - -kv_store_type=etcd
        - -kv_store_host=${DOCKER_HOST_IP}
        - -kv_store_port=2379
        - -banner=true
    volumes:
    - "/var/run/docker.sock:/tmp/docker.sock"
    networks:
    - default

networks:
  default:
    driver: bridge
